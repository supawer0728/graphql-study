type Post {
    id: Int!
    title: String!
    body: String!
    user: User!
    comments(count: Int): [Comment!]!
}

type Comment {
    id: Int!
    name: String!
    email: String!
    body: String!
}
